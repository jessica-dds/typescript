type Email = string;

type Endereco = {
    rua: string,
    cep: string,
    cidade: string,
    endereco: string
}

type Usuario = {
    nome: string,
    idade: number,
    email: Email,
    cpf: string,
    endereco: Endereco
};

const cadastro2 = (info: Usuario) => {
};

const cadastro3 = (info: Usuario) => {
};